<div class="container global">
  <div class="display">
    <h1>Vos réservations</h1>
  </div>

  <div>
    <ol>
      <% @bookings.each do |booking| %>
      <li><%= booking.car.brand %> <%= booking.car.model %> du <%= booking.start_date %> au <%= booking.end_date %>
      <%= link_to "Annuler", booking_path(booking), method: :delete, data: { confirm: "Are you sure?" }, class: 'backlink' %></li>
      <% end %>
    </ol>
  </div>
</div>
